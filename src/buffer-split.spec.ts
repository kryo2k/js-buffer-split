import { ISplitAdapterReader, SplitAdapter, SplitEqualAmount, SplitEqualLength } from './buffer-split';
import { expect } from 'chai';
import 'mocha';

describe('BufferSplit', () => {
  it('should let me split a buffer', () => {
  });
});
